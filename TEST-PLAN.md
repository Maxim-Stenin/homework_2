# Тест-план «Мои финансы» (ДЗ №2)

> **Ревизия 2 — 2026-08-29, сессия 5.** План написан до кода и потому называл
> элементы будущих фич «по роли». Фичи слиты в `main`, и ревизия 2 подставляет
> **фактические `id`**, снятые с живого приложения на стенде (шаг 2 процедуры
> `workflows/qa-cycle.md`, приём «инвентаризация интерфейса вместо макетов»).
> Исходный код приложения при этом не читался — инвентарь снят из DOM в браузере,
> то есть со стороны пользователя.
> Изменения ревизии 2: переписана §1.5, уточнены ПР-F-01, ПР-F-06, ПР-F-16,
> ПР-T-01, ПР-N-05 и ПР-S-01 под фактическую разметку; добавлены ПР-F-17,
> ПР-T-08, ПР-A-01…ПР-A-03; добавлена находка **Н-15**. Ожидания не смягчались:
> ни одно «ожидаемо» не переписано под то, что показало приложение.

Написан **до кода**, по `SPEC.md` (часть А) и по фактическому состоянию `app/`
на момент разметки зон. Автор плана — ветка `docs/test-plan`; кода не пишет
и не проверяет собственную работу.

План — рабочий документ, а не формальность. По нему проводится ручное тестирование
и по нему же пишутся автотесты на `@playwright/test`. Каждая проверка названа
идентификатором, который годится и для баг-репорта, и для имени теста.

- **Часть 1** — как проверяем: стенд, фикстуры, чем доказываем.
- **Часть 2** — проверки по фичам (можно применять к одной ветке, не дожидаясь остальных).
- **Часть 3** — негативные и граничные проверки.
- **Часть 4** — регресс.
- **Часть 5** — таблица соответствия `SPEC.md` → пункты плана.
- **Часть 6** — что планом не покрывается и почему.
- **Часть 7** — находки: расхождения `SPEC.md` с реальностью.
- В конце — **порядок применения плана**: что и когда прогонять.

---

# Часть 1. Как проверяем

## 1.1. Стенд

Ветка `docs/test-plan` работает на порту **8085** (`AGENTS.md`, таблица портов).
Готовые фичи после слияния в `main` проверяются главной сессией на **8080**.
Порт — единственное, что меняется; команда одна.

```bash
py -m http.server 8085 --directory app --bind 127.0.0.1
```

Живость стенда проверяется **по ответу**, а не по факту запуска команды:

```bash
curl -si http://127.0.0.1:8085/index.html | head -1
```

Ожидается `HTTP/1.0 200 OK`.

**Проверено при написании плана (2026-08-28), вывод:**

```
$ curl -si http://127.0.0.1:8085/index.html | head -1
HTTP/1.0 200 OK
$ curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8085/styles.css
200
$ curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8085/app.js
200
```

Замечания по стенду:

- `file://` под Playwright и браузерными агентами заблокирован — страница открывается
  только через сервер.
- Стенд отдаёт UTF-8, консоль PowerShell декодирует как cp1251. Русский текст в выводе
  `curl` выглядит мусором — **это дефект наблюдателя**. Кодировка проверяется в браузере,
  а не в консоли. Баг-репорт «кракозябры в консоли» не заводится.
- Команды пишутся через `py`, не через `python`.

## 1.2. Браузеры и разрешения

- Основной прогон — Chromium (ручной Chrome и Playwright).
- Ширины: 1280×800 (десктоп), 768 (планшет, дополнительно), 375×812 (мобильный,
  обязательно по Сц-14).
- Тема ОС переключается для Сц-13; в Playwright — эмуляция `prefers-color-scheme`
  (`page.emulateMedia`), вручную — настройками системы или переопределением в DevTools
  (Rendering → Emulate CSS media feature `prefers-color-scheme`).
  **Эмуляция DevTools и реальная смена темы ОС — разные пути**; Сц-13 засчитывается
  только при проверке хотя бы одним из них с записанным способом.

## 1.3. Фикстуры данных

Данные кладутся одним из двух способов, способ фиксируется в отчёте:

- **Ф-способ-A (быстро, для автотестов):** `localStorage.setItem('finance.csv', <текст>)`
  до загрузки страницы, затем перезагрузка. Ключ и формат — те же, что у приложения.
- **Ф-способ-B (ближе к пользователю):** «Загрузить CSV» из файла. Требует подтверждения
  в `window.confirm` — в Playwright обрабатывается через `page.on('dialog')`.

**Фикстура Ф1 — базовый набор.** Даты подобраны под период `2026-08-01 … 2026-08-20`:
две записи ровно на границах, две внутри, две снаружи. Все даты — не в будущем
относительно 2026-08-28 (иначе `checkDate` отклонит их и при импорте).

```
date,type,category,amount,comment
2026-08-01,expense,Продукты,1000.00,граница «с»
2026-08-05,income,Зарплата,50000.00,доход внутри
2026-08-10,expense,Кафе,500.00,внутри
2026-08-20,expense,Такси,300.00,граница «по»
2026-08-25,expense,Аптека,200.00,вне периода
2026-08-28,income,Подработка,7000.00,доход вне периода
```

Ожидаемые итоги по Ф1 без фильтра: доходы 57 000,00 ₽; расходы 2 000,00 ₽;
остаток 55 000,00 ₽.

Ожидаемые итоги по Ф1 при периоде `2026-08-01 … 2026-08-20` (границы включаются):
доходы 50 000,00 ₽; расходы 1 800,00 ₽; остаток 48 200,00 ₽; в таблице 4 строки.

**Фикстура Ф2 — регистр категорий.** К Ф1 добавляется запись
`2026-08-12,expense, кафе ,250.00,регистр и пробелы` — по `categoryKey` она обязана
слиться с «Кафе».

**Фикстура Ф3 — пустая:** хранилище очищено, записей нет.

## 1.4. Чем доказывается результат

У каждой проверки ниже указан код артефакта. Проверка без артефакта не считается
пройденной.

| Код | Артефакт | Как снимается |
|---|---|---|
| А1 | Снимок экрана | скриншот области или страницы целиком; для адаптива — с указанной шириной |
| А2 | Фрагмент разметки | `document.getElementById('<id>').outerHTML` в консоли, либо `page.innerHTML()` в тесте |
| А3 | Содержимое хранилища | `localStorage.getItem('finance.csv')` и `Object.keys(localStorage)` |
| А4 | Содержимое выгруженного файла | скачанный `transactions.csv`, показывается текстом целиком |
| А5 | Вывод команды | `curl -si …`, `ls`, `grep`, вывод раннера тестов |
| А6 | Список сетевых запросов | вкладка Network после жёсткой перезагрузки; в тесте — `page.on('request')` |
| А7 | Консоль браузера | полный список сообщений уровня error/warning за прогон |
| А8 | Вывод автотеста | вывод `@playwright/test` целиком, включая падения; падения не пересказываются |

**Правило снятия текстовых артефактов:** значения читаются из DOM
(`textContent` конкретного `id`), а не «видно на картинке». Картинка прикладывается
дополнительно, а не вместо.

## 1.5. Опорные точки в разметке

**Ревизия 2 (2026-08-29).** Инвентарь снят с живого приложения на стенде 8080:
39 элементов с `id`. По правилам проекта эти `id` не переименовываются, поэтому
проверки и автотесты цепляются за них, а не за тексты и координаты.

### Существовали до фич

`sumIncome`, `sumExpense`, `sumBalance`, `addForm`, `fType`, `fAmount`, `fCategory`,
`fDate`, `fComment`, `catList`, `formError`, `filters`, `tabExpense`, `tabIncome`,
`donut`, `legend`, `chartEmpty`, `btnSave`, `btnLoad`, `fileInput`, `tbody`, `tableEmpty`.

### Появились вместе с фичами

| `id` | Тег | Роль | Чей |
|---|---|---|---|
| `themeSwitch` | `div[role=group]` | группа переключателя темы, `aria-label="Тема оформления"` | theme |
| `themeAuto` | `button` | режим «Авто», `data-theme-value="auto"`, состояние в `aria-pressed` | theme |
| `themeLight` | `button` | режим «Светлая», `data-theme-value="light"` | theme |
| `themeDark` | `button` | режим «Тёмная», `data-theme-value="dark"` | theme |
| `storageAlert` | `p.storage-alert` | сообщение об отказе записи в хранилище | storage |
| `filtersToggle` | `button` | кнопка «Фильтры», `aria-expanded`, `aria-controls="filtersPanel"` | filters |
| `filtersCount` | `span` | счётчик активных фильтров внутри кнопки | filters |
| `filtersPanel` | `div` | раскрывающаяся панель фильтров | filters |
| `filterType` | `select` | тип: «Все» / «Доходы» / «Расходы» | filters |
| `filterCategory` | `select` | категория; пустое значение = «Все категории» | filters |
| `filterFrom` | `input[type=date]` | граница периода «с» | filters |
| `filterTo` | `input[type=date]` | граница периода «по» | filters |
| `filtersReset` | `button` | «Сбросить фильтры» — способ вернуться к полному списку | filters |
| `filtersError` | `p.filters-error` | ошибка фильтра (перевёрнутый период) | filters |
| `filtersStale` | `div[role=status]` | плашка «Результат фильтрации устарел» | filters |
| `filtersRefresh` | `button` | «Обновить» на плашке | filters |
| `filtersClose` | `button` | крестик на плашке, `aria-label="Закрыть плашку"` | filters |

### Что важно знать про эти элементы заранее

- **Видимость задаётся атрибутом `hidden`.** Он стоит на `storageAlert`,
  `filtersCount`, `filtersPanel`, `filtersStale`, `fileInput`. Поэтому проверка
  «элемент скрыт» ведётся **не по наличию `hidden`, а по вычисленному стилю
  и коробке**: `getComputedStyle(el).display` и `getBoundingClientRect()`.
  Атрибут может стоять, а элемент быть виден — см. находку **Н-15**.
- **Отдельного элемента под «пустой результат фильтрации» в разметке нет.**
  Сц-4 проверяется по тексту `#tableEmpty`: он обязан отличаться
  от «Записей пока нет.» (ПР-F-06).
- **Способ вернуться к полному списку — `#filtersReset`.**
- Колонки таблицы: «Дата», «Категория», «Комментарий», «Сумма» и безымянная
  колонка с кнопкой удаления.
- Вкладки диаграммы состояние держат в `aria-pressed`, а гашение — в `disabled`.

## 1.6. Шаблон записи проверки

```
ПР-<фича>-<номер> · <название>
Покрывает: Сц-N (или «сверх SPEC»)
Предусловие: <фикстура и состояние экрана>
Шаги: 1) … 2) …
Ожидаемо: <проверяемое утверждение, а не «работает»>
Артефакт: А2 (#tbody), А1
```

---

# Часть 2. Проверки по фичам

Разделы независимы: раздел 2.1 применим к ветке `feature/filters` до слияния
остальных, 2.2 — к `feature/theme`, и так далее. Раздел 2.0 применим уже сейчас,
к коду без фич.

## 2.0. Базовое поведение (применимо к любой ветке)

**ПР-B-01 · Добавить доход и расход, посмотреть итоги**
Покрывает: **Сц-1**
Предусловие: Ф3 (пусто), фильтры не заданы.
Шаги: 1) добавить расход: 1200, «Продукты», сегодня; 2) добавить доход: 5000,
«Зарплата», сегодня.
Ожидаемо: `#sumIncome` = `5 000,00 ₽`, `#sumExpense` = `1 200,00 ₽`,
`#sumBalance` = `3 800,00 ₽`; в `#tbody` две строки; обе записи есть в `finance.csv`.
Артефакт: А2 (`#sumIncome`, `#sumExpense`, `#sumBalance`, `#tbody`), А3, А1.

**ПР-B-02 · Диаграмма учитывает обе записи**
Покрывает: **Сц-1** (часть про диаграмму)
Предусловие: состояние после ПР-B-01.
Шаги: 1) убедиться, что активна вкладка «Расходы по категориям»; 2) прочитать легенду;
3) переключиться на «Доходы по категориям»; 4) прочитать легенду.
Ожидаемо: на вкладке расходов — сегмент «Продукты» 1 200,00 ₽ (100 %);
на вкладке доходов — «Зарплата» 5 000,00 ₽ (100 %); `#chartEmpty` скрыт на обеих.
**Оговорка:** донат показывает один тип за раз, поэтому «диаграмма учитывает обе записи»
из Сц-1 читается как «каждая запись видна на своей вкладке», а не «обе на одной».
См. Часть 7, находка Н-1.
Артефакт: А2 (`#legend` на обеих вкладках), А1×2.

**ПР-B-03 · Пустое состояние без записей**
Покрывает: сверх SPEC (опора для Сц-4)
Предусловие: Ф3.
Ожидаемо: `#tableEmpty` виден, текст «Записей пока нет.»; `#chartEmpty` виден;
итоги — три нуля.
Артефакт: А2 (`#tableEmpty`, `#chartEmpty`), А1.

## 2.1. Фильтрация (ветка `feature/filters`)

Предусловие для всего раздела, если не сказано иное: **Ф1**, страница только что
загружена, фильтры не заданы.

**ПР-F-01 · Панель фильтров открывается и закрывается**
Покрывает: сверх SPEC (опора для Сц-2 и Сц-14)
Шаги: 1) нажать `#filtersToggle`; 2) нажать `#filtersToggle` повторно.
Ожидаемо: `#filtersPanel` раскрывается (вычисленный `display` ≠ `none`, коробка
ненулевая) и содержит ровно три фильтра — `#filterType`, `#filterCategory`,
пару `#filterFrom`/`#filterTo`; **четвёртого фильтра нет** (ни поиска
по комментарию, ни диапазона сумм); `aria-expanded` на кнопке переключается
`false → true → false`; повторное нажатие закрывает панель.
Артефакт: А2 (`#filters`, значения `aria-expanded` и `display`), А1×2.

**ПР-F-02 · Фильтр по типу «Расходы»**
Покрывает: **Сц-2**
Шаги: 1) открыть панель; 2) выбрать тип «Расходы».
Ожидаемо, все пять утверждений проверяются отдельно:
1. в `#tbody` только расходы — 4 строки Ф1 (Продукты, Кафе, Такси, Аптека);
2. `#sumExpense` = `2 000,00 ₽`, `#sumIncome` = `0,00 ₽`, `#sumBalance` = `−2 000,00 ₽`;
3. диаграмма показывает расходы; сумма легенды = 2 000,00 ₽;
4. `#tabIncome` **неактивна** (`disabled`), но **присутствует в разметке** и не скрыта
   (`hidden` не выставлен, элемент есть в DOM);
5. счётчик на кнопке «Фильтры» = **1**.
Артефакт: А2 (`#tbody`, `#tabIncome` целиком с атрибутами, кнопка «Фильтры»), А1.

**ПР-F-03 · Фильтр по типу «Доходы» — зеркальная проверка**
Покрывает: **Сц-2** (вторая половина)
Ожидаемо: в таблице 2 дохода; `#sumIncome` = `57 000,00 ₽`, `#sumExpense` = `0,00 ₽`;
`#tabExpense` неактивна и видна; диаграмма показывает доходы; счётчик = 1.
Артефакт: А2 (`#tbody`, `#tabExpense`), А1.

**ПР-F-04 · Возврат фильтра типа в «все» снимает гашение вкладок**
Покрывает: сверх SPEC (обратимость Сц-2)
Шаги: после ПР-F-02 вернуть тип «все».
Ожидаемо: обе вкладки активны, переключаются вручную; счётчик = 0
(или счётчик не показан — способ показа нуля фиксируется как факт, см. ПР-F-16).
Артефакт: А2 (`#tabExpense`, `#tabIncome`, кнопка «Фильтры»).

**ПР-F-05 · Период охватывает часть записей, границы включаются**
Покрывает: **Сц-3**
Шаги: 1) задать «с» = `2026-08-01`, «по» = `2026-08-20`.
Ожидаемо: в `#tbody` ровно 4 строки — записи от 01, 05, 10 и 20 августа;
записей от 25 и 28 августа нет; **обе граничные даты в списке есть**;
итоги: доходы `50 000,00 ₽`, расходы `1 800,00 ₽`, остаток `48 200,00 ₽`;
счётчик = 2.
Артефакт: А2 (`#tbody`, три итога, кнопка), А1.

**ПР-F-06 · Пустой результат фильтрации — отдельное состояние**
Покрывает: **Сц-4**
Шаги: 1) задать период `2026-08-01 … 2026-08-05`; 2) выбрать категорию «Аптека»
(она существует в записях, но в этот период не попадает).
Ожидаемо: `#tbody` пуст; `#tableEmpty` виден и его текст **отличается
от «Записей пока нет.»** — по смыслу говорит о пустом результате фильтрации;
способ вернуться к полному списку — кнопка `#filtersReset`, она видна и работает:
нажатие возвращает все 6 записей Ф1 и обнуляет счётчик.
**Предварительное условие проверки:** «Аптека» должна оставаться доступной в списке
категорий при заданном периоде. Если список категорий пересобирается по уже
отфильтрованным записям, сценарий в этой формулировке невыполним — см. находку **Н-2**.
Артефакт: А2 (`#tableEmpty` или заменивший его элемент — с текстом; контейнер `#filters`), А1.

**ПР-F-07 · Добавление записи мимо фильтра — устаревший результат**
Покрывает: **Сц-5**
Шаги: 1) задать тип «Расходы»; 2) добавить **доход** 999, «Премия», датой `2026-08-02`.
Дата выбрана заведомо старой намеренно: таблица сортируется по дате, и «первой строкой»
запись может оказаться только в обход обычной сортировки.
Ожидаемо: 1) новая запись — **первая строка** `#tbody`, а не там, куда её положила бы
сортировка по дате; 2) появилась плашка «Результат фильтрации устарел» с кнопкой
«Обновить» и крестиком; 3) `#sumIncome` остался `0,00 ₽` — **итоги запись не учли**;
4) диаграмма не изменилась (легенда прежняя). Расхождение строки и суммы — ожидаемое
поведение, дефектом не заводится.
Артефакт: А2 (первая строка `#tbody`, плашка, три итога, `#legend`), А1.

**ПР-F-08 · «Обновить» пересобирает список**
Покрывает: **Сц-6**
Предусловие: состояние после ПР-F-07.
Шаги: нажать «Обновить».
Ожидаемо: посторонняя запись ушла из таблицы (но **осталась в данных** — проверяется
по `finance.csv` и по снятию фильтра); плашка исчезла; фильтр остался «Расходы»;
счётчик = 1; итоги и диаграмма не изменились.
Артефакт: А2 (`#tbody`, `#filters`), А3, А1.

**ПР-F-09 · Крестик закрывает только плашку**
Покрывает: **Сц-7**
Предусловие: повторить ПР-F-07 (плашка на месте).
Шаги: нажать крестик.
Ожидаемо: плашка исчезла; **список не пересобран** — посторонняя запись по-прежнему
первой строкой; итоги её по-прежнему не учитывают; фильтр активен.
Артефакт: А2 (`#tbody`, `#filters`, итоги), А1.

**ПР-F-10 · Удаление при активном фильтре**
Покрывает: **Сц-8**
Предусловие: Ф1, фильтр «Расходы».
Шаги: 1) удалить запись «Кафе» (подтвердить в диалоге).
Ожидаемо: фильтр **не сброшен** (тип остался «Расходы», счётчик = 1);
появилась плашка «Результат фильтрации устарел»; запись исчезла из таблицы
и из `finance.csv`.
**Уточнение для баг-репорта:** по `SPEC.md` плашка появляется и здесь, хотя список
после удаления фильтру не противоречит. Это дословное требование, поэтому проверяется
как написано; сомнение записано в находку **Н-3**.
Артефакт: А2 (`#tbody`, плашка, кнопка), А3.

**ПР-F-11 · Выгрузка CSV при активном фильтре**
Покрывает: **Сц-9**
Предусловие: Ф1, период `2026-08-01 … 2026-08-20`.
Шаги: нажать «Сохранить CSV», открыть скачанный `transactions.csv`.
Ожидаемо: в файле заголовок `date,type,category,amount,comment` и **ровно 4 строки** —
те же, что на экране; записей от 25 и 28 августа нет; порядок и формат полей прежние;
суммы с двумя знаками (`1000.00`); файл начинается с BOM.
Артефакт: А4 (файл текстом целиком), А2 (`#tbody` для сверки).

**ПР-F-12 · Загрузка CSV сбрасывает фильтр**
Покрывает: **Сц-10**
Предусловие: Ф1, фильтр «Расходы» + период.
Шаги: 1) «Загрузить CSV», выбрать файл Ф1; 2) подтвердить замену в диалоге.
Ожидаемо: фильтр сброшен — тип «все», категория не выбрана, обе границы периода пусты,
счётчик = 0; в таблице все 6 записей; плашка «устарел» **не** показана;
итоги — полные (57 000 / 2 000 / 55 000).
Артефакт: А2 (`#filters` целиком, `#tbody`, итоги), А1.

**ПР-F-13 · Отмена загрузки не трогает фильтр**
Покрывает: сверх SPEC (граница Сц-10)
Шаги: 1) при активном фильтре нажать «Загрузить CSV»; 2) **отменить** в диалоге.
Ожидаемо: фильтр остался как был, список не изменился, в `#formError` — сообщение
об отмене.
Артефакт: А2 (`#filters`, `#formError`).

**ПР-F-14 · Фильтр не переживает перезагрузку**
Покрывает: **Сц-11**
Шаги: 1) задать тип «Расходы» и период; 2) перезагрузить страницу (F5).
Ожидаемо: после загрузки видны все 6 записей; панель фильтров в исходном состоянии;
счётчик = 0; **в `localStorage` нет ключа с состоянием фильтра** — состав ключей
ограничен `finance.csv` и ключом темы.
Артефакт: А3 (`Object.keys(localStorage)`), А2 (`#tbody`, `#filters`).

**ПР-F-15 · Фильтр по категории**
Покрывает: сверх SPEC (третий фильтр из состава, отдельного сценария нет)
Шаги: выбрать категорию «Кафе».
Ожидаемо: в таблице только записи категории «Кафе»; итоги и диаграмма пересчитаны
по ним; счётчик = 1.
Артефакт: А2 (`#tbody`, итоги, кнопка).

**ПР-F-16 · Счётчик активных фильтров, полный перебор**
Покрывает: сверх SPEC (таблица «Ожидания…», строка про счётчик)
Шаги: последовательно довести фильтр до всех четырёх составляющих и снять их обратно.
Ожидаемо, по таблице ожиданий (тип ≠ «все», категория, **каждая** заполненная граница —
по единице):

| Состояние фильтра | Счётчик |
|---|---|
| ничего не задано | 0 (или счётчик скрыт) |
| только тип «Расходы» | 1 |
| тип + категория | 2 |
| тип + категория + «с» | 3 |
| тип + категория + «с» + «по» | 4 |
| только «по» | 1 |
| всё снято обратно | 0 (или скрыт) |

Способ показа нуля (`0` или скрытая метка) `SPEC.md` не задаёт — фиксируется как факт
реализации, дефектом не заводится, но должен быть **одинаков** во всех переходах.
**Ревизия 2:** счётчик — `#filtersCount` внутри `#filtersToggle`; в разметке
на нём стоит `hidden`, поэтому «скрыт» проверяется по вычисленному `display`
и коробке, а не по атрибуту (см. **Н-15**). Читается `textContent` элемента.
Артефакт: А2 (`#filtersToggle` целиком на каждом шаге — 7 снимков разметки
плюс `display` и размеры `#filtersCount`).

**ПР-F-17 · «Сбросить фильтры» возвращает исходное состояние**
Покрывает: сверх SPEC (`#filtersReset` — способ выхода из Сц-4)
Предусловие: Ф1, заданы все три фильтра (состояние ПР-N-11).
Шаги: нажать `#filtersReset`.
Ожидаемо: `#filterType` = «Все», `#filterCategory` = пустое значение,
`#filterFrom` и `#filterTo` пусты; счётчик = 0; в таблице все 6 записей Ф1;
итоги полные (57 000 / 2 000 / 55 000); обе вкладки диаграммы активны;
`#filtersError` пуст; плашка `#filtersStale` не видна.
Артефакт: А2 (`#filtersPanel`, `#tbody`, итоги, вкладки), А1.

## 2.2. Тёмная тема (ветка `feature/theme`)

**ПР-T-01 · Три состояния переключателя**
Покрывает: сверх SPEC (опора для Сц-12 и Сц-13)
Ожидаемо: `#themeSwitch` содержит ровно три кнопки — `#themeAuto`, `#themeLight`,
`#themeDark`, и никакой четвёртой; каждая доступна с клавиатуры; текущее состояние
видно **и** выражено в разметке: `aria-pressed="true"` ровно у одной из трёх.
Артефакт: А2 (`#themeSwitch` целиком с атрибутами), А1.

**ПР-T-08 · `aria-pressed` следует за выбором**
Покрывает: сверх SPEC (состояние видно программно, а не только цветом)
Шаги: нажать по очереди «Светлая», «Тёмная», «Авто».
Ожидаемо: после каждого нажатия `aria-pressed="true"` ровно у нажатой кнопки,
у двух других — `false`; двух «включённых» одновременно не бывает.
Артефакт: А2 (`#themeSwitch` после каждого шага — 3 снимка).

**ПР-T-02 · Тёмная тема переживает перезагрузку**
Покрывает: **Сц-12**
Шаги: 1) выбрать «тёмная»; 2) перезагрузить (F5); 3) перезагрузить ещё раз.
Ожидаемо: после обеих перезагрузок тема тёмная и переключатель показывает «тёмная»;
выбор лежит в `localStorage` **отдельным ключом**, `finance.csv` не изменён —
содержимое до и после совпадает байт в байт.
Артефакт: А3 (`Object.keys(localStorage)` + `finance.csv` до и после), А1.

**ПР-T-03 · Светлая тема так же переживает перезагрузку**
Покрывает: сверх SPEC (симметрия Сц-12)
Ожидаемо: выбранная «светлая» держится и **не уступает** системной тёмной —
явный выбор сильнее системы.
Артефакт: А1, А3.

**ПР-T-04 · Режим «авто» следует за системой без перезагрузки**
Покрывает: **Сц-13**
Шаги: 1) выбрать «авто» при светлой системной теме; 2) **не перезагружая страницу**
переключить систему на тёмную; 3) вернуть светлую.
Ожидаемо: оформление меняется сразу в обе стороны; переключатель остаётся в «авто»;
в консоли пусто.
Способ смены темы записывается: настройки ОС либо эмуляция `prefers-color-scheme`.
Артефакт: А1×3 (до, после, возврат), А7.

**ПР-T-05 · Порча ключа темы не задевает деньги**
Покрывает: сверх SPEC (требование «не внутри данных транзакций»)
Шаги: 1) записать в ключ темы мусор (`'зелёная'`); 2) перезагрузить.
Ожидаемо: приложение открывается, тема — разумное умолчание (авто), записи целы,
`finance.csv` не изменён, консоль без исключений.
Артефакт: А3, А7, А1.

**ПР-T-06 · Контраст текста в обеих темах**
Покрывает: сверх SPEC (таблица «Ожидания…», контраст 4.5:1)
Точки замера: заголовок; подпись `.sub`; значения итогов (`#sumIncome`, `#sumExpense`,
`#sumBalance`, включая отрицательный остаток); текст таблицы; подпись суммы **внутри
кольца диаграммы**; легенда; текст ошибки `#formError`; текст на кнопках, включая
погашенную вкладку.
Ожидаемо: везде не ниже 4.5:1.
**Особое внимание — подписи внутри кольца:** её цвет задан в коде константой
(`fill = '#1c1e21'`), выше черты зон, и по разметке зон никем не правится, см. **Н-4**.
Артефакт: А1 + таблица замеров (пара цветов и вычисленный коэффициент на каждую точку).

**ПР-T-07 · Розового нет ни в одной теме**
Покрывает: сверх SPEC (жёсткая рамка дизайна, проверяется и после темы)
Ожидаемо: ни в палитре диаграммы, ни в акцентах, ни в состояниях наведения нет розового,
фуксии и мадженты — в обеих темах.
Артефакт: А1 (диаграмма с максимальным числом сегментов в обеих темах) + список
фактических значений палитры из CSS/JS.

## 2.3. Вёрстка, адаптив и доступность (ветка `feature/design`)

**ПР-D-01 · Ширина 375 px: одна колонка, нет горизонтальной прокрутки**
Покрывает: **Сц-14**
Предусловие: Ф1 плюс запись с длинными категорией (32 символа — предельная)
и комментарием (~200 символов).
Шаги: 1) выставить 375×812; 2) прокрутить страницу сверху донизу; 3) открыть панель
фильтров.
Ожидаемо: 1) блоки идут одной колонкой; 2) `document.documentElement.scrollWidth`
не превышает `clientWidth` — горизонтальной прокрутки нет **ни на одном экране,
включая раскрытую панель фильтров и таблицу с длинным комментарием**;
3) панель фильтров доступна и её элементы не обрезаны; 4) кнопки удаления нажимаемы.
Артефакт: А1 (несколько снимков по высоте страницы), А2 (значения `scrollWidth`
и `clientWidth`).
**Оговорка:** таблица из пяти колонок — самое узкое место; если она вынесена
в собственный горизонтальный скролл, это допустимо, но **прокрутка страницы целиком —
нет**. Различие фиксируется явно.

**ПР-D-02 · Ширина 1280 px: заявленная раскладка**
Покрывает: сверх SPEC (требование «блоки сужаются, справа кнопка Фильтры»)
Ожидаемо: блоки диаграммы и истории сужены, кнопка «Фильтры» и панель — справа от них;
кнопка показывает счётчик.
Артефакт: А1, А2 (`#filters` и соседи).

**ПР-D-03 · Основной путь с клавиатуры**
Покрывает: **Сц-16**
Шаги: от адресной строки только `Tab`/`Shift+Tab`/`Enter`/`Space`/стрелки:
1) заполнить форму и добавить запись; 2) открыть панель фильтров; 3) задать фильтр;
4) закрыть панель; 5) переключить вкладку диаграммы; 6) переключить тему;
7) дойти до кнопки удаления строки.
Ожидаемо: на каждом интерактивном элементе **виден** фокус (не только `:focus`,
но и видимое кольцо); порядок обхода соответствует зрительному; панель фильтров
открывается и закрывается с клавиатуры; ловушек фокуса нет; погашенная вкладка
фокус не перехватывает.
Артефакт: А1 на каждый шаг (7 снимков с видимым фокусом), список порядка обхода.

**ПР-D-04 · Анимации: длительность и `prefers-reduced-motion`**
Покрывает: сверх SPEC (таблица «Ожидания…»)
Ожидаемо: заявленные переходы длятся 150–200 мс; при `prefers-reduced-motion: reduce`
анимации выключены, а функциональность сохранена (панель всё так же открывается).
Артефакт: А2 (значения `transition-duration` из вычисленных стилей), А1 при включённом
`reduce`.

**ПР-D-05 · Длинные значения не ломают раскладку**
Покрывает: сверх SPEC (граничное)
Предусловие: категория ровно 32 символа, комментарий ~200 символов,
сумма `99999999` (предел `MAX_AMOUNT`).
Ожидаемо: текст переносится или обрезается многоточием, соседние колонки не выдавлены,
подпись внутри кольца остаётся внутри отверстия и читаема.
Артефакт: А1 на 375 и 1280 px.

## 2.4. Отказ хранилища (ветка `feature/storage`)

**ПР-S-01 · Переполнение квоты: сообщение видно, запись на экране**
Покрывает: **Сц-15**
Предусловие: Ф1. Квота забивается **посторонним** ключом, а не порчей `finance.csv`:
в цикле пишется `localStorage.setItem('ballast-N', 'x'.repeat(...))` до исключения,
затем один-два балласта удаляются, чтобы освободить места меньше, чем нужно
приложению.
Шаги: 1) забить хранилище; 2) добавить обычную запись через форму.
Ожидаемо: 1) в `#storageAlert` видно сообщение, прямо говорящее, что запись
**не сохранена** и **пропадёт при перезагрузке** (проверяется по смыслу,
а не дословно; видимость — по вычисленному `display`, не по атрибуту `hidden`);
2) запись **осталась на экране** — есть в `#tbody`, учтена в итогах; 3) `finance.csv`
в хранилище **не содержит** новой записи и **не испорчен** — старые записи целы
и читаются; 4) в консоли нет необработанного исключения.
Артефакт: А2 (`#tbody`, сообщение), А3 (`finance.csv` до и после), А7, А1.

**ПР-S-02 · После освобождения места сохранение восстанавливается**
Покрывает: сверх SPEC (обратимость Сц-15)
Шаги: 1) из состояния ПР-S-01 удалить балласт; 2) добавить ещё одну запись;
3) перезагрузить.
Ожидаемо: сообщение об отказе снято; обе записи (та, что не сохранилась, и новая)
попали в `finance.csv` — или, если реализация сохраняет только новую, **это названо
явно и подтверждено артефактом**; после перезагрузки экран совпадает с хранилищем.
Артефакт: А3, А2, А1.
**Оговорка:** `SPEC.md` не описывает судьбу несохранённой записи после того,
как место освободилось. Проверяем фактическое поведение и записываем; повод
переспросить, а не сразу дефект. См. **Н-5**.

**ПР-S-03 · Приватный режим отличим от переполнения**
Покрывает: сверх SPEC (требование «перестают быть неразличимыми»)
Шаги: открыть приложение в приватном окне (или с заблокированным доступом
к `localStorage`), добавить запись.
Ожидаемо: отказ **виден** (тихого проглатывания нет ни в одном из случаев);
приложение продолжает работать в памяти; консоль без необработанных исключений.
Артефакт: А1, А7.

**ПР-S-04 · Удаление и загрузка CSV при отказе хранилища**
Покрывает: сверх SPEC (`save()` зовётся из трёх мест)
Шаги: при забитой квоте 1) удалить запись; 2) загрузить CSV.
Ожидаемо: отказ записи виден и в этих путях тоже, а не только при добавлении;
экран не расходится с сообщением.
Артефакт: А1, А3, А2.

## 2.5. Начальное состояние экрана (добавлено ревизией 2)

Раздел появился после инвентаризации: четыре элемента интерфейса управляются
атрибутом `hidden`, и ни один сценарий `SPEC.md` не проверяет их состояние
**на чистой загрузке**. Между тем это первое, что видит пользователь.

**ПР-A-01 · Чистая загрузка: временных элементов на экране нет**
Покрывает: сверх SPEC (следствие Сц-5, Сц-7, Сц-15)
Предусловие: Ф3 (`localStorage` очищен), страница загружена заново.
Ожидаемо: **не видны** — плашка `#filtersStale`, сообщение `#storageAlert`,
панель `#filtersPanel`, поле `#fileInput`. Видимость проверяется по вычисленному
`display` и по `getBoundingClientRect()`: коробка нулевая. Виден `#tableEmpty`
с текстом «Записей пока нет.», виден `#chartEmpty`, итоги — три нуля.
Артефакт: А2 (таблица «элемент → `display` → размеры» на все элементы с `hidden`),
А1 (страница целиком), А3 (`Object.keys(localStorage)`).

**ПР-A-02 · Чистая загрузка с данными не поднимает плашку**
Покрывает: сверх SPEC (плашка — следствие действия пользователя, а не загрузки)
Предусловие: Ф1 положена в хранилище, страница загружена заново.
Ожидаемо: 6 строк в таблице, итоги 57 000 / 2 000 / 55 000; плашка `#filtersStale`
не видна; `#storageAlert` не виден; счётчик = 0.
Артефакт: А2, А1.

**ПР-A-03 · Атрибут `hidden` действительно скрывает**
Покрывает: сверх SPEC (механизм, на котором держатся Сц-5, Сц-7, Сц-15)
Шаги: для каждого элемента с `hidden` снять вычисленный стиль.
Ожидаемо: при наличии атрибута `hidden` вычисленный `display` = `none`
и коробка нулевая — **у всех** таких элементов, без исключений.
Артефакт: А2 (список элементов с `hidden` и их `display`).

---

# Часть 3. Негативные и граничные проверки

Следуют из таблицы «Ожидания там, где описание молчит». Все пункты этой таблицы
приняты **агентом**, а не человеком: расхождение — повод переспросить, а не сразу
заводить дефект. В баг-репорте это указывается явно.

**ПР-N-01 · Граница периода включается — точная проверка**
Основание: «с» и «по» включаются.
Шаги: период `2026-08-01 … 2026-08-01`.
Ожидаемо: ровно одна запись — «Продукты» от 01.08; итоги равны её сумме.
Артефакт: А2 (`#tbody`, итоги).

**ПР-N-02 · Пустая граница «по» — открытый интервал вправо**
Основание: пустое поле — интервал открыт с этой стороны.
Шаги: «с» = `2026-08-20`, «по» пусто.
Ожидаемо: показаны записи от 20, 25 и 28 августа — 3 строки; счётчик = 1.
Артефакт: А2 (`#tbody`, кнопка).

**ПР-N-03 · Пустая граница «с» — открытый интервал влево**
Шаги: «с» пусто, «по» = `2026-08-05`.
Ожидаемо: записи от 01 и 05 августа — 2 строки; счётчик = 1.
Артефакт: А2.

**ПР-N-04 · Обе границы пусты**
Ожидаемо: период не фильтрует, все 6 записей; вклад периода в счётчик = 0.
Артефакт: А2.

**ПР-N-05 · Период задом наперёд**
Основание: «показывается ошибка, список не меняется».
Шаги: «с» = `2026-08-20`, «по» = `2026-08-01`.
Ожидаемо: 1) в `#filtersError` видна ошибка, объясняющая, что начало позже конца;
2) **список не изменился** — остался тем, что был до ввода (не «пусто» и не «всё»);
3) итоги и диаграмма тоже не изменились.
**Неоднозначность:** засчитывается ли такой период в счётчик — `SPEC.md` не говорит.
Фактическое поведение фиксируется, дефект не заводится. См. **Н-6**.
Артефакт: А2 (текст ошибки, `#tbody` до и после), А1.

**ПР-N-06 · Исправление перевёрнутого периода**
Шаги: из состояния ПР-N-05 поменять «по» на `2026-08-25`.
Ожидаемо: ошибка исчезла, список пересобран по корректному периоду.
Артефакт: А2.

**ПР-N-07 · Категории сопоставляются по `categoryKey`**
Основание: регистр и пробелы не различаются.
Предусловие: **Ф2**.
Шаги: выбрать в фильтре категорию «Кафе».
Ожидаемо: в таблице **обе** записи — «Кафе» 500,00 и « кафе » 250,00;
в легенде диаграммы — **один** сегмент на 750,00 ₽, а не два;
в списке категорий фильтра «Кафе» встречается **один раз**, а не дважды.
Артефакт: А2 (`#tbody`, `#legend`, список категорий фильтра).

**ПР-N-08 · Категория, стёртая из данных, не остаётся в фильтре**
Основание: категории производны от транзакций.
Шаги: 1) выбрать категорию «Аптека»; 2) удалить единственную запись «Аптека».
Ожидаемо: «Аптека» исчезает из списка категорий фильтра; приложение не остаётся
в состоянии «выбрана несуществующая категория» без объяснения — либо фильтр
по категории сбрасывается, либо показывается пустой результат фильтрации с выходом.
**Поведение `SPEC.md` не задано** — фиксируется как факт, повод переспросить. См. **Н-7**.
Артефакт: А2 (список категорий, `#tbody`), А1.

**ПР-N-09 · Фильтр «все + пусто + пусто» ничего не меняет**
Ожидаемо: экран совпадает с состоянием до открытия панели: те же 6 строк,
те же итоги, обе вкладки активны, плашка «устарел» не появилась, счётчик 0.
Артефакт: А2 (`#tbody`, итоги, вкладки).

**ПР-N-10 · Дата вне допустимого при активном фильтре**
Шаги: при активном фильтре попытаться добавить запись с датой в будущем.
Ожидаемо: отказ в `#formError` («Дата не может быть в будущем.»); запись не создана;
фильтр не тронут; плашка «устарел» **не** появилась.
Артефакт: А2 (`#formError`, `#tbody`, `#filters`).

**ПР-N-11 · Комбинация всех трёх фильтров**
Шаги: тип «Расходы» + категория «Кафе» + период `2026-08-01 … 2026-08-20`.
Ожидаемо: одна строка (Кафе 10.08, 500,00); расходы `500,00 ₽`, доходы `0,00 ₽`;
`#tabIncome` погашена; счётчик = 4.
Артефакт: А2 (`#tbody`, итоги, вкладки, кнопка).

**ПР-N-12 · Плашка «устарел» не размножается**
Шаги: при активном фильтре добавить подряд три неподходящие записи.
Ожидаемо: плашка одна, а не три; все три записи видны сверху таблицы;
итоги их не учитывают.
Артефакт: А2 (`#filters`, `#tbody`).

**ПР-N-13 · Смена фильтра снимает устаревание**
Шаги: 1) довести до состояния ПР-F-07 (плашка видна); 2) изменить фильтр
(например, добавить период).
Ожидаемо: список пересобран по новому фильтру, плашка снята — устаревший результат
не может пережить пересборку.
**`SPEC.md` этого не описывает** — фиксируем факт. См. **Н-8**.
Артефакт: А2 (`#tbody`, `#filters`).

---

# Часть 4. Регресс — что фичи не должны сломать

Прогоняется **после каждого слияния** ветки в `main`, а не один раз в конце.

**ПР-R-01 · Ключ хранения не изменился**
Ожидаемо: данные лежат в `localStorage` под ключом ровно `finance.csv`;
никакой другой ключ данные транзакций не содержит; ключ темы — отдельный и другой.
Артефакт: А3 (`Object.keys(localStorage)` и значение `finance.csv`).

**ПР-R-02 · Формат CSV не изменился: файл старой версии читается новой**
Предусловие: файл Ф1, сохранённый до начала работ.
Шаги: загрузить его в новую версию.
Ожидаемо: все 6 записей приняты, ни одна строка не потеряна, суммы и даты совпадают.
Артефакт: А4 (исходный файл), А2 (`#tbody`), А1.

**ПР-R-03 · Формат CSV не изменился: выгрузка совпадает по структуре**
Ожидаемо: заголовок ровно `date,type,category,amount,comment`; порядок полей прежний;
суммы с двумя знаками; экранирование кавычками работает для категории
и комментария с запятой, кавычкой и переводом строки; BOM на месте.
Проверочная запись: комментарий из трёх строк вида `а, "б"` с переводом строки внутри.
Артефакт: А4 (файл текстом), А2.

**ПР-R-04 · Круговой обход выгрузка → загрузка**
Шаги: выгрузить без фильтра, очистить хранилище, загрузить обратно.
Ожидаемо: состояние совпадает с исходным — те же записи, те же итоги.
Артефакт: А4, А2 (итоги до и после), А3.

**ПР-R-05 · Лимит суммы не изменился**
Ожидаемо: `99999999` принимается; `100000000` отклоняется сообщением
«Сумма не может превышать 99 999 999.»; `0`, `0.001`, `-5`, `12abc`, `1_000`,
`1e5`, `1,2,3`, `12 руб` отклоняются; введённое **не искажается** молча.
Артефакт: А2 (`#formError` и `#fAmount` после каждой попытки — таблица вход/результат).

**ПР-R-06 · Лимит категории не изменился**
Ожидаемо: 32 символа принимаются; 33 отклоняются сообщением
«Категория не длиннее 32 символов.»; поле **не обрезает** введённое молча
(`maxlength` не появился); пробелы по краям срезаются.
Артефакт: А2 (`#formError`, `value` поля `#fCategory`).

**ПР-R-07 · Валидация даты и типа не изменилась**
Ожидаемо: пустая дата, `2026-02-30`, дата в будущем — отклоняются своими сообщениями;
`max` у `#fDate` равен сегодняшней дате.
Артефакт: А2.

**ПР-R-08 · Импорт по-прежнему «всё или ничего»**
Шаги: загрузить файл, где 3-я строка битая.
Ожидаемо: отказ с указанием номера строки; **список не изменён**; хранилище не тронуто.
Артефакт: А2 (`#formError`, `#tbody`), А3.

**ПР-R-09 · Подтверждения не пропали**
Ожидаемо: удаление и замена списка при загрузке по-прежнему спрашивают подтверждение;
отказ в диалоге ничего не меняет.
Артефакт: А1 (диалоги), А2.

**ПР-R-10 · Наружу ничего не ходит**
Шаги: жёсткая перезагрузка с открытой вкладкой Network, затем полный проход
основного пути.
Ожидаемо: запросов ровно три — `index.html`, `styles.css`, `app.js`, все с 127.0.0.1;
**нет** запросов к шрифтам, CDN, аналитике; нет `fetch`/`XHR`/WebSocket.
Артефакт: А6 (полный список запросов), А5 (`curl` по трём адресам).

**ПР-R-11 · Файлов в `app/` по-прежнему три**
Ожидаемо: `index.html`, `styles.css`, `app.js` — и ничего больше;
четвёртого файла не появилось.
Артефакт: А5 (`ls app`).

**ПР-R-12 · Консоль чистая**
Ожидаемо: за полный прогон разделов 2.0–2.4 в консоли нет сообщений уровня error;
warning — перечисляются с объяснением, почему допустимы.
Артефакт: А7 (полный вывод консоли за прогон).

**ПР-R-13 · Существующие `id` не переименованы**
Ожидаемо: все `id` из п. 1.5 присутствуют в разметке.
Артефакт: А5 (`grep -o 'id="[^"]*"' app/index.html` — список целиком).

**ПР-R-14 · Данные пользователя переживают обновление**
Шаги: положить в `finance.csv` данные, снятые с предыдущей версии, открыть новую.
Ожидаемо: записи прочитаны, ни одна не потеряна, итоги совпадают.
Артефакт: А3, А2.

---

# Часть 5. Соответствие «сценарий SPEC.md → пункты плана»

Ни один из 16 сценариев не остался без проверки. Столбец «сверх сценария» —
дополнительные проверки, которые опираются на тот же сценарий.

| Сц | Формулировка `SPEC.md` | Основные пункты | Сверх сценария |
|---|---|---|---|
| Сц-1 | Добавить доход и расход, посмотреть итоги | ПР-B-01, ПР-B-02 | ПР-B-03 |
| Сц-2 | Фильтр по типу «Расходы» | ПР-F-02 | ПР-F-03, ПР-F-04, ПР-F-01 |
| Сц-3 | Период, границы включаются | ПР-F-05 | ПР-N-01…ПР-N-04 |
| Сц-4 | Пустой результат фильтрации | ПР-F-06 | ПР-B-03, ПР-N-08 |
| Сц-5 | Запись мимо фильтра, устаревший результат | ПР-F-07 | ПР-N-12, ПР-N-10 |
| Сц-6 | «Обновить» на плашке | ПР-F-08 | ПР-N-13 |
| Сц-7 | Крестик на плашке | ПР-F-09 | — |
| Сц-8 | Удаление при активном фильтре | ПР-F-10 | ПР-R-09 |
| Сц-9 | Выгрузка CSV при фильтре | ПР-F-11 | ПР-R-03, ПР-R-04 |
| Сц-10 | Загрузка CSV при фильтре | ПР-F-12 | ПР-F-13, ПР-R-08 |
| Сц-11 | Перезагрузка с активным фильтром | ПР-F-14 | ПР-R-01 |
| Сц-12 | Тёмная тема после перезагрузки | ПР-T-02 | ПР-T-01, ПР-T-03, ПР-T-05 |
| Сц-13 | «Авто» следует за системой | ПР-T-04 | ПР-T-01 |
| Сц-14 | Экран 375 px | ПР-D-01 | ПР-D-02, ПР-D-05 |
| Сц-15 | Отказ записи в хранилище | ПР-S-01 | ПР-S-02, ПР-S-03, ПР-S-04 |
| Сц-16 | Основной путь с клавиатуры | ПР-D-03 | ПР-T-01, ПР-F-01 |

Обратная сверка — пункты плана, **не названные ни в одной строке таблицы выше**
(это норма: они закрывают таблицу «Ожидания там, где описание молчит» и регресс,
а не сценарии):

ПР-F-15, ПР-F-16, **ПР-F-17**, ПР-T-06, ПР-T-07, **ПР-T-08**, ПР-D-04,
**ПР-A-01**, **ПР-A-02**, **ПР-A-03**, ПР-N-05, ПР-N-06, ПР-N-07,
ПР-N-09, ПР-N-11, ПР-R-02, ПР-R-05, ПР-R-06, ПР-R-07, ПР-R-10, ПР-R-11,
ПР-R-12, ПР-R-13, ПР-R-14 — итого 24 (жирным — добавленные ревизией 2).

Арифметика сходится: всего проверок **67** (ПР-B ×3, ПР-F ×17, ПР-T ×8, ПР-D ×5,
ПР-S ×4, ПР-A ×3, ПР-N ×13, ПР-R ×14); 43 из них названы в таблице сценариев,
24 — здесь, 67 = 43 + 24. До ревизии 2 было 62. Пересечений между списками нет: пункт, стоящий
в столбце «сверх сценария», в этот список не попадает (ПР-F-13 привязан к Сц-10,
ПР-N-01…ПР-N-04 — к Сц-3, ПР-R-01, ПР-R-03, ПР-R-04, ПР-R-08, ПР-R-09 —
к Сц-11, Сц-9, Сц-9, Сц-10 и Сц-8 соответственно).

## 5.1. Соответствие критериям приёмки продукта

| Критерий из `SPEC.md` | Чем закрывается |
|---|---|
| Все 16 сценариев проходят, каждый с артефактом | Часть 5, столбец «основные пункты» |
| Работает без сетевых запросов наружу | ПР-R-10 |
| Ключ `finance.csv` и формат CSV не сломаны | ПР-R-01, ПР-R-02, ПР-R-03, ПР-R-04, ПР-R-14 |
| Контраст ≥ 4.5:1, розового нет | ПР-T-06, ПР-T-07 |
| 375 px без горизонтальной прокрутки | ПР-D-01 |
| Отказ записи виден (Сц-15) | ПР-S-01 |
| В консоли нет ошибок | ПР-R-12 |

---

# Часть 6. Что планом не покрывается и почему

Названо явно: умолчание о непроверенном приравнивается к ложному отчёту.

1. **Кросс-браузерность.** Прогон только в Chromium. Firefox и Safari не проверяются:
   стенд один, времени на три браузера нет, а `SPEC.md` требует лишь «современный
   браузер». Риск — расхождения по `<input type="date">` и `prefers-color-scheme`.
2. **Реальные мобильные устройства.** Проверяется эмуляция 375 px, а не физический
   телефон. Не покрыты: поведение экранной клавиатуры, реальные попадания пальцем,
   `100vh` при плавающей адресной строке.
3. **Производительность и объём.** По допущению `SPEC.md` — сотни записей.
   Десятки тысяч записей, скорость фильтрации и отрисовки диаграммы не измеряются.
4. **Две вкладки одновременно.** Прямо исключено допущениями `SPEC.md`:
   рассинхронизация между вкладками и событие `storage` не проверяются.
5. **Скринридеры.** Проверяется фокус с клавиатуры (Сц-16) и наличие `aria-label`,
   но не озвучивание NVDA/VoiceOver. Формально доступность закрыта частично.
6. **Точная формулировка пользовательских текстов.** Проверяется смысл сообщений,
   а не дословный текст: `SPEC.md` задаёт смысл, а не строки. Исключение — тексты,
   которые уже есть в коде и упомянуты в `SPEC.md` дословно
   («Результат фильтрации устарел», сообщения о лимитах сумм и категорий).
7. **Юнит-тесты `app.js`.** План описывает поведение приложения. Отдельные проверки
   чистых функций (`checkAmount`, `parseCSV`, `groupByCategory`) — задача набора
   автотестов, а не этого плана; здесь их поведение проверяется через UI.
8. **Пункты `SPEC.md` «Чего точно не делаем».** Отсутствие справочника категорий,
   поиска по комментарию, фильтра по суммам, редактирования записи, сортировки
   и пагинации проверяется только тем, что они **не появились** (ПР-F-01);
   отдельных проверок нет.
9. **Установка и настройка `@playwright/test`.** На момент написания плана он
   не установлен (`SPEC.md`, открытые вопросы). План написан так, чтобы годиться
   и для ручного прогона; при откате на pytest пункты переносятся механически.
10. **Печать, экспорт в PDF, офлайн-режим, установка как PWA** — вне объёма продукта.

---

# Часть 7. Находки: расхождения `SPEC.md` с реальностью

Собраны при написании плана, до кода. `SPEC.md` не правится — это решение человека.
Каждая находка — либо повод уточнить спецификацию, либо ожидаемое поведение,
которое нельзя принимать за дефект.

**Н-1 · Сц-1 требует от диаграммы невозможного.**
«Итоги и диаграмма учитывают обе записи», но донат по устройству показывает **один
тип за раз** (`groupByCategory(chartMode)`, вкладки «Расходы»/«Доходы»). Одновременно
доход и расход на одной диаграмме не появятся. В плане сценарий читается как
«каждая запись видна на своей вкладке» (ПР-B-02). Требуется уточнение формулировки.

**Н-2 · Сц-4 может оказаться невыполнимым в собственной формулировке.**
Сценарий требует выбрать «категорию, которой нет ни в одной записи внутри периода»,
а состав фильтра — «одна из существующих в записях». Если реализация наполнит
список категорий уже отфильтрованными записями, такой категории в списке
не окажется, и сценарий пройти нельзя. Нужно зафиксировать: список категорий фильтра
строится по **всем** записям, а не по отфильтрованным. В плане это записано
предварительным условием ПР-F-06.

**Н-3 · Сц-8: плашка «Результат фильтрации устарел» после удаления.**
После удаления показанный список фильтру не противоречит — устаревания в смысле
Сц-5 нет. Требование выглядит как перестраховка. Проверяем дословно (ПР-F-10),
но формулировка «устаревает показанный результат» с этим случаем плохо согласуется.

**Н-4 · Подпись внутри кольца диаграммы не принадлежит ни одной зоне.**
В `app.js`, функция `renderChart`, цвет подписи задан жёстко:
`label.setAttribute('fill', '#1c1e21')`. Это **выше черты зон**, а из исключений
выше черты у `feature/design` только константа `COLORS`, а `feature/theme` выше черты
не пишет вообще. Следствие: в тёмной теме тёмная подпись на тёмном фоне,
контраст 4.5:1 (ПР-T-06) не достигается, **и по разметке зон это чинить некому**.
Нужно решение человека: расширить исключение для `feature/design`
или для `feature/theme`.

**Н-5 · Судьба несохранённой записи после освобождения места не описана.**
`SPEC.md` говорит, что запись остаётся на экране и пользователь предупреждён,
но не говорит, что происходит, когда место появилось: попадёт ли она в хранилище
при следующем `save()`. Проверяем фактическое поведение (ПР-S-02).

**Н-6 · Счётчик при перевёрнутом периоде не определён.**
Период задом наперёд даёт ошибку и не меняет список, но считать ли его границы
в счётчике активных фильтров — не сказано. Фиксируем факт (ПР-N-05).

**Н-7 · Выбранная категория, исчезнувшая из данных.**
Удаление последней записи категории при активном фильтре по ней `SPEC.md`
не описывает. Фиксируем факт (ПР-N-08).

**Н-8 · Снятие плашки при смене фильтра не описано.**
Указаны только два способа убрать плашку — «Обновить» и крестик. Что происходит
при изменении самого фильтра, не сказано (ПР-N-13).

**Н-9 · Устаревшая ссылка на строку кода.**
`SPEC.md`, раздел 4: «Сейчас `save()` (`app/index.html:441`)». После разделения
на три файла (сессия 4) `save()` живёт в `app/app.js`, а `app/index.html` — 143 строки,
и строки 441 в нём нет. Сам код `save()` при этом совпадает с процитированным.

**Н-10 · Устаревший объём файла.**
`SPEC.md`, «Исходное состояние»: «одностраничное web-приложение, `app/index.html`,
766 строк». Фактически: `index.html` 143, `styles.css` 155, `app.js` 556 — всего 854.
Раздел «Допущения» разделение на три файла уже описывает, то есть спецификация
противоречит сама себе.

**Н-11 · Порт в критериях приёмки.**
Критерий приёмки называет `http://127.0.0.1:8080/index.html`. Для веток в worktrees
`AGENTS.md` закрепляет порты 8081…8085 — этот план проверяется на **8085**.
Противоречия нет, но 8080 в критерии стоит читать как «стенд главной сессии»,
иначе критерий формально невыполним из рабочей копии.

**Н-12 · Имена файла и ключа легко перепутать.**
Ключ хранения — `finance.csv`, а выгружаемый файл называется `transactions.csv`.
В `SPEC.md` фигурирует только `finance.csv`, из-за чего Сц-9 можно ошибочно
проверять не по тому имени. В плане имена разведены явно (ПР-F-11, ПР-R-01).

**Н-13 · Порядок строк с одинаковой датой не определён.**
Сортировка в `renderTable` — `a.date < b.date ? 1 : -1`, для равных дат компаратор
никогда не возвращает 0. Ожидание `SPEC.md` «по дате, новые сверху» одинаковые даты
не различает, поэтому взаимный порядок таких строк в плане **не проверяется**
и дефектом не считается.

**Н-14 · «Диаграмма показывает выбранный тип» — механизм не назван.**
При фильтре «доходы», когда открыта вкладка «Расходы», `SPEC.md` требует, чтобы
диаграмма показала доходы, но не говорит, переключается ли `chartMode` автоматически
и что будет со вкладкой после возврата фильтра в «все». В плане проверяется
наблюдаемый результат (ПР-F-02, ПР-F-03, ПР-F-04), а не способ его достижения.

**Н-15 · Атрибут `hidden` в этом приложении не гарантирует скрытость.**
Найдено при инвентаризации ревизии 2, до прогона. Атрибут `hidden` стоит
на пяти элементах, но у двух из них вычисленный `display` его перебивает:
`#filtersStale` → `display: flex`, коробка 320×73 px; `#filtersCount`
→ `display: block`, коробка 20×18 px. У остальных трёх (`#storageAlert`,
`#filtersPanel`, `#fileInput`) — `display: none`, как и ожидается.
Следствие для плана: **проверка «элемент скрыт» ведётся по вычисленному стилю
и коробке, а не по наличию атрибута.** Проверки, которые опираются на скрытость
(ПР-F-08, ПР-F-09, ПР-F-16, ПР-S-01, ПР-A-01…ПР-A-03), переформулированы
соответственно. Само расхождение — кандидат в дефект и заводится по итогам
прогона, а не находкой: находка описывает противоречие в документации,
а здесь противоречие между разметкой и стилями.

---

## Порядок применения плана

1. Раздел 2.0 и Часть 4 — применимы уже сейчас, к коду без фич: это база сравнения.
2. После слияния каждой ветки — её раздел из Части 2 плюс Часть 4 целиком.
3. Часть 3 — после того, как фильтрация слита: без неё большинство пунктов бессмысленно.
4. Итоговый прогон на `main`: Части 2, 3 и 4 подряд, с артефактами на каждый пункт.

**Проверяет не автор кода.** Ветка `docs/test-plan` кода не пишет, поэтому ручной
прогон по этому плану выполняется ею же — это и есть «независимый взгляд»
из правил проекта.
