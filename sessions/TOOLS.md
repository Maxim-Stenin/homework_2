# Журнал инструментов

> Только дополняется, старые записи не редактируются. Сюда попадает всё, что вы
> добавили в проект или окружение ради этой задачи: библиотеки, CLI-утилиты,
> линтеры, MCP-серверы, плагины и скиллы агента, хуки, внешние сервисы, контейнеры.
>
> Разовый вызов уже установленного инструмента сюда не пишется — он идёт
> в таблицу инструментов внутри файла сессии.
>
> Тупиковая попытка — тоже запись: поставили, не подошло, сняли. Это как раз интересно.

## Шаблон записи

```markdown
## YYYY-MM-DD · Сессия N · <инструмент> vX.Y

- **Тип:** библиотека / CLI / MCP / плагин / скилл / хук / сервис
- **Установка:** `<точная команда>`
- **Зачем:** какую задачу решает
- **Область:** проект или глобально
- **Проверка:** чем убедились, что работает
```

Удалённый или заменённый инструмент фиксируется так же — с пометкой
**удалён** / **заменён на X** и причиной.

---

## 2026-08-24 · Сессия 1 · Typeless 2.3.1

- **Тип:** сервис (голосовой ввод)
- **Установка:** `Typeless-2.3.1-x64-Setup.exe`, установлен человеком до начала сессии
- **Зачем:** промпты в ДЗ №2 длинные; надиктовать быстрее, чем набрать. Показан
  на занятии 2 как рекомендованный инструмент, засчитывается отдельным необязательным
  пунктом задания
- **Область:** глобально, на машине человека
- **Проверка:** первый промпт этой сессии надиктован через него; текст пришёл связным,
  без слов-паразитов

## 2026-08-24 · Сессия 1 · Superpowers v6.3.0 — перенос шести скиллов

- **Тип:** скиллы (перенос файлами, не установка плагина)
- **Установка:** плагин **НЕ ставился**. Репозиторий https://github.com/obra/superpowers
  прочитан отдельным субагентом-проверяющим, shallow-клон во временном каталоге сессии;
  шесть скиллов скопированы в `.claude/skills/`. Источник, ревизия и причина —
  в `.claude/skills/SOURCE.md`, лицензия MIT — в `LICENSE-superpowers`
- **Зачем:** `dispatching-parallel-agents` и `subagent-driven-development` — под
  обязательный пункт 2 (параллельные субагенты); `writing-plans` и
  `verification-before-completion` — совпадают с правилами курса «сначала план»
  и «проверка вместо слова готово»; `requesting-code-review` — «автор не проверяет
  сам себя»; `using-git-worktrees` — необязательный пункт в плюс
- **Область:** проект (едут в репозиторий вместе со сдачей)
- **Проверка:** `.claude/skills/<имя>/SKILL.md` — шесть каталогов на месте, 128 КБ.
  Три шелл-скрипта `subagent-driven-development/scripts/` (127 строк) проверены
  на сеть, `eval`, `base64`, `rm -rf`, чтение домашнего каталога — не найдено ничего
  из перечисленного. Работоспособность скиллов в деле **не проверена**: ни один
  из них в этой сессии не вызывался

## 2026-08-24 · Сессия 1 · SUPERPOWERS_DISABLE_TELEMETRY=1

- **Тип:** переменная окружения
- **Установка:** блок `env` в `.claude/settings.json`, вместе с `DISABLE_TELEMETRY=1`
- **Зачем:** у Superpowers есть сетевой beacon — логотип с домена автора
  (`primeradiant.com`) в визуальном компаньоне брейншторма; уходят IP, версия, время.
  Выставлено до переноса, а не после
- **Область:** проект
- **Проверка:** прямой проверки нет и быть не может — компаньон мы не переносили,
  срабатывать нечему. Записано как мера предосторожности, а не как доказанный факт

## 2026-08-26 · Сессия 2 · Node.js 24.19.0 (LTS) + npm 11.17.0

- **Тип:** среда выполнения
- **Установка:** `winget install OpenJS.NodeJS.LTS --accept-source-agreements --accept-package-agreements --silent`
  (установщик MSI запросил права администратора, подтверждение дал человек)
- **Зачем:** решение человека в сессии 2 после сравнения двух подходов к проверкам.
  Node нужен под `@playwright/test` — единственный вариант, при котором функции
  `app.js` (`checkAmount`, `parseCSV`, `groupByCategory`) можно покрыть юнит-тестами
  напрямую, без браузера. Побочно открывает браузерные MCP-серверы, которые
  без Node не работают
- **Область:** система (не проект)
- **Проверка:** `node --version` → `v24.19.0`, `npm --version` → `11.17.0`.
  Важно: в уже открытой сессии PowerShell `PATH` не обновляется — версии получены
  после явного перечитывания машинного и пользовательского `PATH`.
  Сам Playwright ещё **не поставлен**: `@playwright/test` и браузеры — отдельный шаг

## 2026-08-26 · Сессия 2 · скилл `frontend-design` (Anthropic)

- **Тип:** скилл ассистента (перенесён файлом)
- **Установка:** `gh api repos/anthropics/claude-code/contents/plugins/frontend-design/skills/frontend-design/SKILL.md --jq '.content' | base64 -d > .claude/skills/frontend-design/SKILL.md`
- **Зачем:** человек отдал дизайн приложения агенту и потребовал вести его
  «по скиллу фронтендера». Скилл задаёт порядок: план из токенов (палитра, шрифты,
  раскладка, «подпись» страницы) → самокритика плана на шаблонность → код
- **Область:** проект
- **Проверка:** файл на месте, 8260 байт, фронтматтер `name: frontend-design` читается.
  **В деле не проверен** — на момент записи по нему ещё ничего не сверстано.
  Источник, ревизия `423563c` и расхождения с правилами проекта — в `.claude/skills/SOURCE.md`

## 2026-08-27 · Сессия 3 · GitNexus 1.6.9 — плагин Claude Code

- **Тип:** плагин ассистента (скиллы + хуки)
- **Установка:**
  `claude plugin marketplace add abhigyanpatwari/GitNexus`
  `claude plugin install gitnexus@gitnexus-marketplace`
  Scope — **user**, то есть глобально на машину. Вариант выбран человеком
  из трёх предложенных; отклонены «CLI + MCP только в проект» и «сначала
  проверить в песочнице».
- **Зачем:** необязательный пункт задания «оптимизатор контекста». Строит граф
  кодовой базы, отдаёт агенту срез вместо сплошного чтения файлов. Выбран после
  сравнения с Graphify — см. разбор в `sessions/session-3.md`
- **Область:** система (`~/.claude`), не проект. Хуки `PreToolUse`/`PostToolUse`
  действуют **во всех проектах человека**, а не только в этом
- **Проверка:** проверено по файлам, а не по слову «Successfully»:
  `~/.claude/settings.json` → `extraKnownMarketplaces.gitnexus-marketplace`
  и `enabledPlugins["gitnexus@gitnexus-marketplace"] = true`;
  хуки на диске — `~/.claude/plugins/cache/gitnexus-marketplace/gitnexus/1.6.9/hooks/hooks.json`.
  **В деле не проверен:** ни один из 12 скиллов не вызывался, граф не строился
- **Разбор перед установкой (сделан до, а не после):** невидимых символов нет,
  флагов `--dangerously-skip-permissions`, `bypassPermissions`, `--yolo`,
  `autoApprove` нет нигде. Хук `gitnexus-hook.js` (572 строки) **в сеть не ходит** —
  только `spawnSync` для локальных `git` и `gitnexus`; текст, который он вставляет
  в контекст агента, информационный («индекс устарел», «локальный доступ занят»),
  без директив и без `permissionDecision: deny`
- **Что осталось непроверенным:** манифест плагина объявляет владельцем
  `nico@gitnexus.dev` и homepage `github.com/nicosxt/gitnexus`, а установка идёт
  из `abhigyanpatwari/GitNexus` — несовпадение в цепочке поставок не выяснено.
  Лицензия распознана GitHub как `NOASSERTION`

## 2026-08-27 · Сессия 3 · GitNexus CLI 1.6.9

- **Тип:** CLI
- **Установка:** `GITNEXUS_SKIP_OPTIONAL_GRAMMARS=1 DO_NOT_TRACK=1 npm install -g gitnexus`
  Флаг пропуска грамматик — потому что без него npm собирает нативные модули
  для Dart, Proto, Swift и Kotlin, а `python3`/`make`/`g++` на машине нет.
  Эти четыре языка парситься не будут; нам они не нужны
- **Зачем:** без CLI плагин — декорация: его хук ищет `gitnexus` через `which`
  и без него молча ничего не делает
- **Область:** система
- **Проверка:** `gitnexus --version` → `1.6.9`, путь
  `/c/Users/msten/AppData/Roaming/npm/gitnexus`. Функциональный замер на копиях
  в scratchpad: `index.html` (766 строк) → 1 узел, 0 связей; тот же JS в `app.js`
  (506 строк) → 63 узла, 154 связи, 5 кластеров, 13 потоков.
  **Вывод: на проекте до разделения на три файла инструмент бесполезен**
- **Найдено при установке:** npm 11 заблокировал все install-скрипты, включая
  `@scarf/scarf` 1.4.0 («Google Analytics для npm-пакетов», шлёт на `scarf.sh`
  платформу, архитектуру, версию Node и состав зависимостей) и `onnxruntime-node`
  (тянет бинарники с `api.nuget.org`). Ни один postinstall не выполнился
- **Опасное поведение, найденное запуском:** `gitnexus analyze` без флагов
  **дописывает `AGENTS.md`, `CLAUDE.md` и шесть скиллов в `.claude/skills/`**
  анализируемого репозитория, с директивами `MUST` / `NEVER`. В `result/`
  запускать **только** `gitnexus analyze --index-only` — флаг описан самим
  вендором как «skip all file injection (AGENTS.md, CLAUDE.md, skills)»

## 2026-08-29 · Сессия 5 · Google Chrome 141 + Node.js 24.19.0 по CDP
- **Тип:** приём работы с уже установленным, **новых установок нет**
- **Установка:** ничего не устанавливалось. Chrome уже был в системе,
  `WebSocket` встроен в Node 24 — внешний клиент не нужен. Скрипты
  `shoot.mjs` (съёмка) и `measure.mjs` (замеры) лежат во временном каталоге
  сессии, в репозиторий не кладутся.
- **Зачем:** получить скриншоты **файлами на диске** для баг-репортов и снять
  замеры во втором окружении. Браузерная панель агента картинку показывает,
  но в файловую систему не пишет; а прогон в одном окружении оказался
  недостаточным доказательством.
- **Проверка:** сняты шесть экранов 2560x1600 и таблица контрастов по шести
  сочетаниям «системная тема x выбранная тема». Именно этот замер отозвал два
  кандидата в дефекты: в панели 1.76:1, в Chrome — 6.81:1.
- **Чего не сделано:** `@playwright/test` по-прежнему не установлен. Для этой
  задачи он не понадобился; решение по раннеру автотестов остаётся за человеком.

## 2026-08-29 · Сессия 5 · Pillow 12.3.0
- **Тип:** библиотека, **уже была** в составе Python 3.14.7
- **Установка:** не требовалась
- **Зачем:** разметка скриншотов красной рамкой в формате баг-репортов человека
- **Проверка:** четыре PNG в `bugs/`, рамка построена по `getBoundingClientRect()`
  элемента, а не на глаз

## 2026-08-29 · Сессия 6 · Встроенная браузерная панель + `py -m http.server`
- **Тип:** приём работы с уже имеющимся, **новых установок нет**
- **Установка:** ничего не устанавливалось. Стенд — `py -m http.server 8080
  --directory app --bind 127.0.0.1`, остановлен по окончании работы
- **Зачем:** проверить семь правок сессии 6 на живом приложении: вычисленные
  стили и коробки элементов с `hidden`, раскладка панели фильтров на 1280 и 375 px,
  тексты пустых состояний и сообщений хранилища
- **Проверка:** ПР-A-01…ПР-A-03, Сц-5 → Сц-6 → Сц-7, ПР-F-06, ПР-N-08, ПР-S-03,
  ПР-S-04 пройдены; в консоли, кроме собственных диагностических `console.error`
  из шагов проверки, ничего нет
- **Найденное ограничение панели:** подставить подмену **до** исполнения скриптов
  страницы она не даёт. Поэтому «хранилище запрещено» проверялось вызовом `load()`
  с подменённым `getItem` уже после загрузки, а не как в сессии 5 через CDP, где
  подмена ставится до навигации. Ветка «испорченное содержимое» проверена честной
  перезагрузкой — ей пре-инъекция не нужна
- **Чего не сделано:** `@playwright/test` по-прежнему не установлен
