# Состояние проекта

> Обновляется в конце каждой сессии. Описывает «как есть сейчас», а не историю
> изменений — старое перезаписывается. Первым делом в новой сессии агент читает
> именно этот файл.

**Обновлено:** 2026-08-28, сессия 4

## Направление и объём

Домашнее задание №2 (занятие 2 от 21.08.2026). Срок сдачи: **29 августа 2026, 18:00**.
Осталось меньше двух суток. Сдаётся ссылкой на репозиторий в телеграм `@oleg56x`.

Репозиторий: <https://github.com/Maxim-Stenin/homework_2>, **публичный**.

Предмет работы — свой пет-проект «Мои финансы» (`app/`: `index.html`,
`styles.css`, `app.js`).
Направление человека — QA.

Обязательны три пункта: работающий результат, работа субагентами, workflow.

## Готово

- Задание и сопроводительные материалы прочитаны целиком (`task/04`, `task/05`).
- `AGENTS.md`: Часть I — правила курса, Часть II — под этот проект. Дополнена
  решениями сессий 1, 2, 3 и 4.
- `CLAUDE.md`, `.gitignore`, `.claude/settings.json` — на месте. В `settings.json`
  блок `env`: `SUPERPOWERS_DISABLE_TELEMETRY=1`, `DISABLE_TELEMETRY=1`,
  **`DO_NOT_TRACK=1`** (добавлен в сессии 3).
- **Пет-проект разделён на три файла** (сессия 4): `app/index.html` 126 строк,
  `app/styles.css` 131, `app/app.js` 504. Было — один файл на 766 строк.
  Проверено в браузере: CSS применяется (52 правила), функции `app.js` определены,
  сквозной путь «добавить расход» проходит, консоль чистая. Коммит `ec38c2a`.
- **Пять веток и пять worktrees заведены** (сессия 4), копии лежат вне репозитория,
  в `../worktrees/`. Порты стендов 8081–8085 закреплены за ветками в `AGENTS.md`,
  8080 остался за `main`. Необязательный пункт «worktrees» взят.
- **Зоны субагентов и порядок слияния зафиксированы** (сессия 4). Границы размечены
  маркерами `ЗОНА <ветка>` в `index.html`, `styles.css` и `app.js`; в `app.js`
  заведён шов `listFor(purpose)`, через который отрисовка и выгрузка берут список.
  Таблица зон, точечные исключения и порядок слияния — в `AGENTS.md`, раздел
  «Субагенты». Коммит `da23d35`, все пять веток на нём.
- **Брифы пяти субагентам написаны** (сессия 4) и лежат в `sessions/session-4.md`:
  общая часть плюс блок на ветку. Ни один субагент не запускался.
- Стенд проверен по ответу: `curl -si … | head -1` → `HTTP/1.0 200 OK`.
- Git инициализирован, `origin` привязан. Работа сессий 1 и 2 запушена,
  `origin/main` = `951aa0d`. Локальный `main` = `ec38c2a`, **на два коммита впереди
  origin**: работа сессии 3 и разделение приложения на три файла. Не запушено.
- Семь скиллов в `.claude/skills/`: шесть из Superpowers v6.3.0 и `frontend-design`
  из репозитория Claude Code. Плагин Superpowers не установлен, скиллы перенесены
  файлами; источники и лицензии — в `.claude/skills/SOURCE.md`.
- `feature-proposals.md` — разбор приложения и вариантов развития (сделан субагентом).
- **Состав работы решён** (сессия 1): вёрстка и дизайн, тёмная тема, фильтрация,
  починка тихой потери данных. Справочник категорий — сознательно не делаем.
- **`SPEC.md`, Часть А, написана** (сессия 2): поведение четырёх фич, 16 сценариев,
  таблица ожиданий с авторством решений, границы, допущения, критерии приёмки.
- **Node.js 24.19.0 LTS + npm 11.17.0 установлены.** Раннер проверок выбран:
  `@playwright/test`.
- **`DESIGN.md` написан** (сессия 3, по скиллу `frontend-design`): токены цвета
  для обеих тем, палитра диаграммы, три типографические роли, раскладка с двумя
  каркасами, движение, самокритика плана на шаблонность, порядок работы.
  **Скилл `frontend-design` применён в деле — первый вызов перенесённого скилла
  за весь проект.**
- **Три решения по дизайну приняты человеком** (сессия 3): кирпичный `#B04527`
  вместо алого для расходов — да; ширина контейнера 980 → 1120 px — да;
  полоса баланса вместо трёх карточек итогов — **нет**, карточки остаются.
- **GitNexus 1.6.9 установлен** — плагин Claude Code (user-scope) и CLI.
  Необязательный пункт «оптимизатор контекста». Разбор до установки, замер после —
  в `sessions/session-3.md` и `sessions/TOOLS.md`.
- **Graphify проверен и отклонён** — вредоносного кода нет, но его хук вставляет
  в контекст агента самораспространяющуюся директиву. Не устанавливался.
- Журнал: `session-1.md`…`session-3.md` (закрыты), `session-4.md` (в работе),
  `TOOLS.md`, `STATE.md`.

## В работе

Сессия 4: разделение приложения на три файла, ветки и worktrees под веер из пяти
субагентов. Кода фич не написано — только подготовка рабочих мест.

## Не начато

- **Пункт 1** — работающий результат. Поведение описано в `SPEC.md`, дизайн описан
  в `DESIGN.md`, кода не написано.
- **Установка `@playwright/test`** и браузеров. Отложена сознательно, решение
  человека, сессия 3: тестировать пока нечего.
- **Пункт 2** — разработка субагентами. Два субагента отработали на разведке
  в сессии 1, веерной разработки не было. Рабочие места под пять субагентов
  готовы (сессия 4), сами субагенты не запускались.
- **Пункт 3** — workflow. `workflows/` пуста. Форма согласована с человеком
  в сессии 3 (цикл QA: анализ документации → тест-план → ручное тестирование →
  автотесты → ревью → ретест → завершение фичи), файла нет.
- **Проверка контраста** пар из `DESIGN.md` инструментом.
- **Подпись страницы** — вариант «цвет категории живёт на всей странице» предложен,
  ответа человека нет.
- `REPORT.md` — не создан. PDF — не создан.
- Из необязательных пунктов взято два: оптимизатор контекста (GitNexus, сессия 3)
  и git worktree (сессия 4).

## Активные инструменты

- git 2.55.0, GitHub CLI 2.97.0 (авторизован как `Maxim-Stenin`)
- **Claude Code CLI 2.1.247 — `~/AppData/Roaming/npm/claude`, в PATH есть**
- Python 3.14.7 — через `py`, не через `python`
- Node.js 24.19.0 LTS, npm 11.17.0
- **GitNexus 1.6.9** — CLI + плагин Claude Code (user-scope, хуки во всех проектах)
- Typeless 2.3.1 — голосовой ввод
- Семь скиллов в `.claude/skills/` плюс двенадцать скиллов GitNexus из плагина

## Известные проблемы

- **Ни один из 16 сценариев `SPEC.md` не проходился.** В сессии 4 приложение
  впервые открыто в браузере, но проверялось ровно одно: что разделение на файлы
  не сломало загрузку стилей, исполнение скрипта и основной путь.
- **`gitnexus analyze` без флага пишет в `AGENTS.md` и `CLAUDE.md` проекта**
  свои директивы (`MUST` / `NEVER`) и кладёт шесть скиллов в `.claude/skills/`.
  Проверено запуском на копии в песочнице. В этом репозитории команда запускается
  **только с `--index-only`** — правило записано в `AGENTS.md`. В `result/`
  `analyze` не запускался ни разу.
- **GitNexus на приложении был бесполезен до сессии 4** — блокировавшее его
  разделение на файлы теперь сделано, повторный замер не проводился. Прежний замер
  на копиях:
  `index.html` (766 строк, JS внутри `<script>`) → 1 узел, 0 связей;
  тот же JS в `app.js` (506 строк) → 63 узла, 154 связи, 5 кластеров, 13 потоков.
  Инструмент не видит JS внутри HTML.
- **В дереве зависимостей GitNexus есть `@scarf/scarf` 1.4.0** — телеметрия
  установок (платформа, архитектура, версия Node, состав зависимостей → `scarf.sh`).
  Не сработала: npm 11 заблокировал все install-скрипты. Против повторения —
  `DO_NOT_TRACK=1` в `settings.json`.
- **Из скиллов в деле проверен только `frontend-design`.** Шесть скиллов Superpowers
  и двенадцать скиллов GitNexus ни разу не вызывались.
- **В скиллах Superpowers есть битые перекрёстные ссылки** на `executing-plans`
  и `finishing-a-development-branch` — их не переносили.
- **Вердикт по безопасности Superpowers неполон, и это записано честно:**
  маркетплейс не проверялся; 13 файлов скиллов целиком не читались; история
  коммитов не анализировалась.
- **Скилл `frontend-design` взят только файлом `SKILL.md`**, без `LICENSE.txt`,
  на который ссылается его фронтматтер.
- **Дефект в приложении не исправлен:** `save()` глотает исключение пустым `catch`,
  переполнение квоты `localStorage` приводит к тихой потере данных.
- **Зоны субагентов проверены только на бумаге.** Пересечения реальны: `app.js`
  правят трое, `index.html` — трое, `styles.css` — двое. Маркеры и шов `listFor()`
  заведены под это, но выдержат ли они работу пяти агентов — выяснится при первом
  слиянии, а не сейчас. Конфликт при merge означает выход за границу зоны.
- **Числа контраста в `DESIGN.md` расчётные, не измеренные.** До проверки
  инструментом утверждать «4.5:1 выдержано» нельзя.
- **Мусор от ошибки агента:** ~19 МБ в `~/AppData/Local/npm-cache/_npx` от
  `npx playwright --version`. Удаление требует `rm -rf`, который в `deny`.
- **После установки Node в уже открытой сессии PowerShell `node --version`
  не работает** — `PATH` процесса не обновляется. Нужен новый терминал.

### Снято в сессии 3

- ~~«Установить плагины из сессии невозможно — `claude` как CLI в PATH отсутствует»~~
  (записано в сессии 2). **Неверно на сегодня:** `claude` в PATH есть, версия
  2.1.247, и плагин GitNexus поставлен именно им. Прошлый журнал не переписывался.

## Следующий шаг

1. Написать workflow в `workflows/` — обязательный пункт 3. Форма согласована
   в сессии 3, документ пишется **до** того, как процедура применяется.
2. Запустить пятого субагента (`docs/test-plan`) с тест-планом — он пишется
   до реализации.
3. Запустить четыре субагента по фичам параллельно в их worktrees.
5. Слить ветки в `main`, прогнать сценарии `SPEC.md`, независимое ревью.
6. Установка `@playwright/test` — `app.js` теперь есть, блокер снят.
7. Ответить на открытый вопрос по «подписи» страницы в `DESIGN.md`
   и замерить контраст пар инструментом.
