# Состояние проекта

> Обновляется в конце каждой сессии. Описывает «как есть сейчас», а не историю
> изменений — старое перезаписывается. Первым делом в новой сессии агент читает
> именно этот файл.

**Обновлено:** 2026-08-24, сессия 1 (закрыта)

## Направление и объём

Домашнее задание №2 (занятие 2 от 21.08.2026). Срок сдачи: **29 августа 2026, 18:00**.
Осталось 5 дней. Сдаётся ссылкой на репозиторий в телеграм `@oleg56x`.

Репозиторий: <https://github.com/Maxim-Stenin/homework_2>, **публичный**.

Предмет работы — свой пет-проект «Мои финансы» (`app/index.html`), перенесённый
из ДЗ №1. Направление человека — QA.

Обязательны три пункта: работающий результат, работа субагентами, workflow.

## Готово

- Задание и сопроводительные материалы прочитаны целиком (`task/04`, `task/05`).
- Разобрано, что из ДЗ №1 переносимо как шаблон, а что специфично для того задания.
- `AGENTS.md`: Часть I перенесена дословно, Часть II написана под ДЗ №2 и дополнена
  решениями сессии 1.
- `CLAUDE.md`, `.gitignore`, `.claude/settings.json` — на месте.
  В `settings.json` есть блок `env` с `SUPERPOWERS_DISABLE_TELEMETRY=1`.
- Пет-проект скопирован в `app/index.html`, `diff` с оригиналом пуст.
- Стенд проверен по ответу: `curl -si … | head -1` → `HTTP/1.0 200 OK`, тело 31 035 байт.
- Git инициализирован, `origin` привязан, первый коммит `9425838` (11 файлов).
  Удалённый репозиторий существует и пуст.
- **Superpowers разобран независимым субагентом.** Плагин **не установлен**
  (`~/.claude/plugins` отсутствует). Шесть скиллов перенесены файлами
  в `.claude/skills/`, источник и лицензия — в `.claude/skills/SOURCE.md`.
- `feature-proposals.md` — разбор приложения и вариантов развития, сделан субагентом.
- Решения по содержанию работы приняты — см. `AGENTS.md`, «Решения по содержанию работы».
- Журнал: `sessions/session-1.md` (закрыта), `TOOLS.md`, `STATE.md`.

## В работе

Ничего. Сессия 1 закрыта, сессия 2 не начата.

## Не начато

- **Разделение `app/index.html` на три файла** (`index.html`, `styles.css`, `app.js`).
  Решение принято и записано в `AGENTS.md`, но не выполнено — файл по-прежнему один.
- **Пункт 1** — работающий результат. Что делаем, решено; кода не написано.
  `SPEC.md` — каркас с `TODO(сессия N)`.
- **Интервью по спецификации.** Человек выступает бизнес-аналитиком, агент задаёт
  вопросы. Не начиналось.
- **Пункт 2** — разработка субагентами. Два субагента отработали на разведке,
  но веерной разработки не было.
- **Пункт 3** — workflow. `workflows/` пуста. Форма процедуры известна со слов
  человека: фича → анализ доки → план проверок → прогон → баги и отчёт →
  ретест после фиксов.
- `REPORT.md` — не создан.
- Необязательные пункты: ни один не взят.
- **Первый push не сделан.** Ждёт команды человека.

## Активные инструменты

- git 2.55.0, GitHub CLI 2.97.0 (авторизован как `Maxim-Stenin`)
- Python 3.14.7 — через `py`, не через `python`
- Typeless 2.3.1 — голосовой ввод
- Шесть скиллов из Superpowers v6.3.0 в `.claude/skills/`
- **Node.js в системе отсутствует.** Решение об установке отложено.

## Известные проблемы

- **Приложение ни разу не открывалось в браузере.** Проверен только факт раздачи
  файла по HTTP. Ни один пользовательский сценарий не проходился — утверждать,
  что приложение работает, оснований нет.
- **Перенесённые скиллы ни разу не вызывались.** Что они работают в этом проекте —
  предположение.
- **В перенесённых скиллах есть битые перекрёстные ссылки** на `executing-plans`
  и `finishing-a-development-branch` — их не переносили. Ссылки намеренно
  не вычищались; наткнувшись на такую, агент останавливается и спрашивает.
- **Вердикт по безопасности Superpowers неполон, и это записано честно:**
  маркетплейс, откуда идёт установка, не проверялся; 13 файлов скиллов целиком
  не читались; `server.cjs` прочитан выборочно, полного аудита path traversal нет;
  история коммитов не анализировалась.
- **Установить плагин из этой сессии невозможно** — `/plugin` открывает
  интерактивную панель, `claude` как CLI в PATH отсутствует. Обошли переносом файлов.
- **Найден дефект в приложении, ещё не исправленный:** `save()` глотает исключение
  пустым `catch`, поэтому переполнение квоты `localStorage` приводит к тихой потере
  данных — запись видна на экране, но в хранилище не легла. Решено чинить.
- **Приложение — один файл на 766 строк**, и это главное ограничение для параллельной
  работы. Снимается разделением на три файла, которое пока не сделано.
- Скиллы `qa-manual` и `qa-autotest` из ДЗ №1 не перенесены: они построены вокруг
  роли «тестировщик, который не правит объект», а здесь она не действует.

## Следующий шаг

1. Push первого коммита — по команде человека.
2. Интервью по спецификации. Первый вопрос — что фильтры делают с итогами
   и диаграммой (человек отложил его до обсуждения фич).
3. Разделение `app/` на три файла — до того, как в него пойдут параллельные агенты.
