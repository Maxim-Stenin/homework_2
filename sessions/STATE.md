# Состояние проекта

> Обновляется в конце каждой сессии. Описывает «как есть сейчас», а не историю
> изменений — старое перезаписывается. Первым делом в новой сессии агент читает
> именно этот файл.

**Обновлено:** 2026-08-24, сессия 1

## Направление и объём

Домашнее задание №2 (занятие 2 от 21.08.2026). Срок сдачи: **29 августа 2026, 18:00**.
Осталось 5 дней. Сдаётся ссылкой на репозиторий в телеграм `@oleg56x`; приватный
репозиторий — доступ на чтение `@gfarhad93`.

Предмет работы — свой пет-проект «Мои финансы» (`app/index.html`), перенесённый
из ДЗ №1. Направление человека — QA.

Обязательны три пункта: работающий результат, работа субагентами, workflow.

## Готово

- Задание и сопроводительные материалы прочитаны целиком (`task/04`, `task/05`).
- Разобрано, что из ДЗ №1 переносимо как шаблон, а что специфично для того задания.
- `AGENTS.md`: Часть I перенесена дословно, Часть II написана под ДЗ №2.
- `CLAUDE.md`, `.gitignore`, `.claude/settings.json` — на месте.
- Пет-проект скопирован в `app/index.html` (766 строк, HTML+CSS+JS, `localStorage`, CSV).
- Журнал заведён: `sessions/session-1.md`, `TOOLS.md`, `STATE.md`.

## В работе

- Сессия 1 не закрыта: не заполнен финальный вердикт.

## Не начато

- **Git не инициализирован.** Сдача требует ссылку на репозиторий — это первый шаг
  следующей сессии.
- `SPEC.md` — только каркас с `TODO(сессия N)`: не решено, что именно делаем
  с пет-проектом.
- **Пункт 1** — работающий результат: содержание работы не выбрано.
- **Пункт 2** — субагенты: не запускались.
- **Пункт 3** — workflow: `workflows/` пуста.
- `REPORT.md` — не создан.
- Необязательные пункты: ни один не выбран (решение человека — по ходу работы).
- Первый коммит: git не инициализирован, история пуста.

## Активные инструменты

- git 2.55.0, GitHub CLI 2.97.0 (установлен, авторизация не проверялась)
- Python 3.14.7 — через `py`, не через `python`
- Typeless 2.3.1 — голосовой ввод

## Известные проблемы

- **Копия приложения проверена только на уровне HTTP.** `diff` с оригиналом пуст,
  стенд поднялся, `curl -si … | head -1` → `HTTP/1.0 200 OK`, тело 31 035 байт.
  Это доказывает, что файл отдаётся, и **не доказывает**, что приложение работает
  в браузере: страницу никто не открывал, сценарии не проходились.
- **Содержание пункта 1 не определено.** Пет-проект есть, но что именно с ним делать —
  новая фича, разбор, набор проверок — не решено. Без этого нельзя написать `SPEC.md`
  и нельзя осмысленно разделить работу между субагентами.
- **Скиллы `qa-manual` и `qa-autotest` из ДЗ №1 не перенесены.** Они привязаны
  к прежней роли «тестировщик, который не правит объект» — в этом проекте она
  не действует. Переносить с правкой или не переносить вовсе — решение человека.

## Следующий шаг

1. Решить, что делаем с пет-проектом — от этого зависит всё остальное.
2. `git init -b main`, первый коммит с шаблоном.
3. Поднять стенд и убедиться по ответу `curl`, что копия жива.
